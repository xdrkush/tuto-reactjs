// require('dotenv').config()

const dbOptions = {
    // host: process.env.HOST,
    host: 'localhost',
    user: 'tuto',
    password: 'tuto$',
    port: 3306,
    database: 'tuto_react'
};

module.exports = dbOptions;
